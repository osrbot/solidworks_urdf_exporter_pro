---
search: false
---

# 点击预览时 SolidWorks 崩溃的排查记录

## 问题

安装 `fe8dc64` 后，点击“预览并导出”会在读取 `base_link` 的质量属性时关闭 SolidWorks。
此前通过的 Mock 测试和安装文件校验没有覆盖这一段真实 COM 调用，不能证明它可以在 SW 中正常运行。

## 已复现的原因

使用 SolidWorks 2023、新建独立进程和仓库示例零件的临时副本，复现了以下问题：

1. 向 `IMassProperty2.SelectedItems` 写入 `null`，会触发 SW 原生接口异常 `0x80010105`。
2. 把组件放进普通 `object[]` 传入同一接口，也会触发原生异常。组件数组需要按 `IDispatch` 封送。
3. 质量属性对象会继承界面中已有的选择。传入空数组不一定清空这个范围，后续可能读取错误的组件。
4. 同一零件的两个实例引用不同配置时，SW 2023 的覆盖标志可能来自零件文档当前配置，而不是所选实例的引用配置。
   数值质量正确，不代表覆盖标志也正确；它会影响后续是否允许自动校准惯性。

第一、二项是崩溃原因。第三、四项是实机回归中发现的额外正确性问题。

## 修复范围

- 整体读取使用非空引用的空数组，不向接口写入 `null`；组件列表使用 `DispatchWrapper[]`。
- 读取时隔离临时选择列表，结束后恢复原来的选择；继续检查实际读取范围，不取消安全校验。
- 从 Link 编辑页进入预览时，屏蔽计算引起的选择回调，避免把临时计算选择写回 Link 的 CAD 绑定。
- 无法确认覆盖标志属于当前引用配置时，明确提示配置不匹配，不自动切换用户配置，也不忽略覆盖值继续导出。
- 保持质量、质心和惯性读取对象的生命周期由 SW 管理，不主动释放共享 COM 对象。

参考：[SW 的 IDispatch 数组传参约定](https://help.solidworks.com/2023/english/api/sldworksapiprogguide/Overview/IDispatch_Object_Arrays_as_Input_in_.NET.htm)、
[保存选择列表](https://help.solidworks.com/2023/english/api/sldworksapi/SOLIDWORKS.Interop.sldworks~SOLIDWORKS.Interop.sldworks.ISelectionMgr~SuspendSelectionList.html)、
[恢复原选择列表](https://help.solidworks.com/2023/english/api/sldworksapi/SOLIDWORKS.Interop.sldworks~SOLIDWORKS.Interop.sldworks.ISelectionMgr~ResumeSelectionList2.html)。

## 验证方式

可重复的实机脚本：`scripts/Test-EffectiveMassOwnedSolidWorks.ps1`。
脚本只操作自己新建且最初没有打开文档的 SW 进程；使用示例零件的临时副本，不打开、保存或删除用户原始装配体。

```powershell
powershell.exe -NoProfile -STA -ExecutionPolicy Bypass `
  -File scripts/Test-EffectiveMassOwnedSolidWorks.ps1 `
  -BuildDll <待测目录>/SW2URDF.dll `
  -InteropDirectory <SW互操作程序集目录>
```

检查零件配置切换、质量/质心/惯性覆盖、组件 A/B/A 反复读取、组件聚合、选择恢复及配置不匹配时的拒绝路径。
这不是完整用户装配体通过界面导出的替代测试。

修复后的验证结果：

- 质量属性读取的聚焦回归：33/33 通过，包括接口传参、范围检查及成功/失败时恢复选择。
- SolidWorks 2023 隔离样件：质量分别为 3.25 kg 和 5 kg 的两个配置来回读取通过；质量、质心、完整惯性矩阵和覆盖标志均符合预期。
- 装配体组件 A/B/A 读取及两组件聚合通过，聚合质量 6.5 kg；质心按实例平移独立计算，聚合惯性按平行轴定理独立检查。
- 读取前后检查了所选对象身份、顺序、选择标记、文档配置和保存状态；配置不匹配路径明确拒绝，没有切换配置。
- “界面选中 B、读取 A”，以及相反方向的交叉选择测试通过，结果没有受原选择影响。
- 实机测试记录：`.codex-build/crash-isolated/live-probe.log` 和 `cross-selection-live.log`。仅关闭了脚本创建的临时装配体和独立 SW 进程。

安装包经过 `BuildInstaller.ps1` 的全量回归门禁，对应的 provenance 文件记录源码版本、测试结果和文件摘要。
旧安装包 `fe8dc64` 有已确认的实机崩溃问题，不应继续使用。

## 安装后界面复测

2026-09-05 在 SolidWorks 2023 SP1.0 中安装并加载 `43392a8`，核对了安装目录的 249 个文件摘要和 COM 注册信息。
测试使用用户装配体的完整副本，未在原件上执行导出或保存。副本包含 156 个文件、10 个 Link。

- 通过 SolidWorks 的“导出为 URDF”命令进入 Link 编辑，再点击原来触发崩溃的“预览并导出”。本次正常打开导出向导，没有发生原来的崩溃。
- 在 1920 x 1080、100% 缩放下查看 Joint 基本、约束与安全，以及 Link 惯性、可视/碰撞、外观和模型导出页，未观察到输入框下边框裁切。此次不能代替 4K、150% 缩放验收。
- 对 `camera_link` 点击“恢复 SW 值”，得到质量 `0.018874996615538962 kg`；开启实测质量校准并输入测试值 `0.02 kg`，完整惯性矩阵按质量比缩放，等效长方体可显示，尺寸为 `32.9 / 32.5 / 23.2 mm`。
- 导出前的惯性校验通过。CSV 明确记录 SW 来源质量、导出质量和按比例校准状态；比较的是经过编辑策略处理的导出值，不是强迫手动质量等于原始 CAD 质量。

### 尚未解决的性能问题

完整装配体的预览准备耗时约 4 分 48 秒，其中 `base_link` 读取约 179 秒；正式导出前的惯性校验又耗时约 7 分 20 秒。
这次验证支持“原来的预览崩溃未再出现”，不支持“整条工作流已经足够流畅”。应继续单独分析大装配体中重复读取有效质量属性的开销。

四目标带网格导出未完成验收：2026-09-05 08:29:29 进入 `base_link` 的 `ModelDocExtension.SaveAs` 调用，截至本次记录时已等待超过 9 分钟，尚未返回，也没有生成该 STL 文件。
进度窗口仍能更新时间，SW 进程仍在消耗 CPU；这些现象不能证明网格生成在正常推进，也不足以判定死锁。没有强制结束 SW，现场保留在测试副本上。

校验 CSV 的通过不等于最终 URDF、USD 或 MJCF 已导出通过。本次没有取得最终四目标结果，也没有完成高 DPI 复测。
原始装配体的 156 个文件已逐一核对 SHA-256，内容未变。

本地证据位于 `.codex-build/gui-43392a8/`（不纳入发布文档资产）：

- `preview-and-export-progress.mp4`：38 秒实机片段，保留点击预览和导出进度，中间等待已省略，不能用来表示实际耗时。
- `desktop-export-test.mp4`、`desktop-export-test-part2.mp4`：导出阶段的原始分段录像，包含独立进度窗口。
- `gui-test.log`、`inertial_validation.csv`：实测日志和导出前惯性校验结果。
- `original-manifest.json`：原始文件摘要；`Verify-Artifacts.ps1 -OriginalsOnly` 可再次核对。

首次按 SW 主窗口录制未能包含独立导出向导，随后改为录制 SW 所在屏幕区域；因此此次录像不是完整的功能页面教程。页面切换与质量校准已实际操作，但对应完整视频仍需在工作流不再阻塞后补录。

## STL 长时间不返回的现场诊断

随后用户截图显示总耗时 `01:38:05`、当前 STL 阶段 `01:30:17`。日志仍然停留在 08:29 开始的同一次 `base_link` 保存，没有生成该 STL 文件，不能视作正常完成或继续无限等待。

对测试 SW 进程进行了短暂、非侵入式调用栈采样，采样后退出调试器并保留 SW 进程，没有修改模型或强制终止进程：

- 托管栈确认主线程位于 `IModelDocExtension.SaveAs`，调用者为 `ExportHelper.SaveStlWithVisibleComponents`。
- 两次原生栈均经过实体三角化 `gciTopEnt_w::tessellate`、`gciBody_w::tessellateForTessellationManager`、`bodyTessellate`，并停留在 `PSKERNEL` 几何内核路径中。
- SW 主线程仍消耗 CPU。因此这不是仅凭动画猜测的“还在工作”，也没有证据认定为线程互相等待的死锁。部分原生符号仅为导出符号加偏移，不能据此认定具体算法或故障零件。
- 当前尚未进入 USD/MJCF 转换阶段；此处也不是惯性矩阵校验拒绝导出。

证据位于同一本地目录的 `stl-hang-stack-1.log`、`stl-hang-stack-2.log`、`stl-hang-managed.log` 和 `stl-hang.dmp`。快照只保留在本地，不作为公开发布资产。

尚未确定具体触发因素：需要在副本中对 `base_link` 组件分组测试，并对比同样的几何和设置下 SW 原生 STL 导出与插件调用。不能仅凭当前现象归咎于复杂模型或网格精度，也不能把外层等待超时当成安全取消原生 COM 调用。

### 新进程对照测试

用户授权继续诊断后，保存现场日志，并在核对 PID、启动时间和程序路径后结束了上述阻塞的测试实例。原始 CAD 文件未保存或修改。

以下每轮均创建独立 SW 实例，只读打开完整测试副本，检查所有已加载文档位于副本目录；使用相同两个根组件、253 个可见组件、米制二进制粗略 STL。每轮设置阶段超时，超时仅结束该轮拥有的实例。

| 测试 | STL 保存耗时 | 结果 |
| --- | --- | --- |
| 直接调用 SW SaveAs | 41.641 秒 | 642,074 面，32,103,784 字节 |
| 先调用已安装插件的两次面数估算 | 41.630 秒 | 相同面数和体积；估算返回 0 |
| 先调用已安装插件的有效质量读取 | 38.907 秒 | 相同面数和体积；质量读取 221.262 秒 |
| 两次惯性预览 Show/Hide 后保存 | 41.442 秒 | 相同面数和体积；未创建临时宿主 |
| 先全部隐藏，再显示 base 组件 | 38.476 秒 | 相同面数和体积 |
| 质量读取期间暂停特征树和视图刷新，随后恢复 | 37.707 秒 | 相同面数和体积；质量读取 103.476 秒 |
| 调用已安装插件的完整 SaveSTL 路径 | 成功 | 相同面数和体积；含构造、坐标系解析和收尾总计 145.362 秒 |

这些是进程外 COM 对照，不能代替 SolidWorks 进程内完整界面工作流。它们排除了“同样几何必然需要 90 分钟”的推断，但没有单独定位原生内核长时间不返回的触发条件。

一次质量测试在进入质量读取之前，被 SW 的“系统内存极低”提示阻塞；该轮被标记为无效对照并由测试超时监护结束，不计为质量读取失败。可见性切换和质量读取的采样另外发现了特征树、视图反复刷新的开销。暂停刷新不改变物理计算及覆盖属性检查；其异常恢复和进程内表现还需验证。

对应证据为本地 `probes/*/stdout.log`、`native-stack.log` 和各轮 `mesh.STL`。此时仍未完成最终四目标安装版验收，不能宣称 90 分钟阻塞已经修复。
