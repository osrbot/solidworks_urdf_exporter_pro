---
search: false
---

# fc88a14 安装后实机复测

测试日期：2026-09-06。环境：SolidWorks 2023 SP1.0，已安装插件 `fc88a146747e`。
使用整车装配体测试副本，不保存或修改原始模型。本次不修改插件代码，不重新打包。

## 已验证

- 安装目录的 249 个文件与安装包清单哈希一致。正常启动 SW 后，插件对象可读取，导出入口可用。此前自动化启动的实例没有加载插件，不应把那次实例当作安装后的界面验收。
- 已安装 DLL 的真实 SW 质量属性测试通过：质量、质心、惯性覆盖，配置切换，组件 A/B/A 选择及聚合读取；读取前后的选择和配置保持一致。
- Link 树框选 camera_link 和 laser 后应用，10 个 Link、9 个 Joint 保留，camera_joint 和 laser_joint 没有被清空。
- “预览并导出”成功打开。在本次窗口尺寸下，Joint 基本页和约束与安全页没有多余滚动条，最下排输入框边线完整。
- 旧配置未记录惯性来源时保留原值，不擅自启用比例校准。手动修改质量后仍可显示等效惯性长方体。
- camera_link 恢复 SW 值后，质量约为 0.018875 kg；校准为 0.02 kg 后矩阵同比变化，等效长方体保持约 32.9 x 32.5 x 23.2 mm。切换 Link 后两种预览均清除，质量编辑值保留。
- 模型页四个输出目标已勾选。取消、恢复 MuJoCo 勾选后布局未错位。
- 完整导出：ROS1、ROS2、OpenUSD、MuJoCo 全部成功，125 个文件，约 326.4 MiB，耗时 5 分 1 秒。
- 底盘原生 STL SaveAs：可视网格 28.839 秒，碰撞网格 21.886 秒，均返回成功、错误码 0。本次没有重现一个多小时停在底盘保存的情况。
- 独立文件检查通过 131 项：URDF 结构和网格引用完整，ROS1/ROS2 的机器人定义一致，四种输出的 camera_link 质量均为 0.02 kg，惯性 CSV 记录了比例校准且无 FAIL。
- 把 USD 和 MuJoCo 输出复制到新目录后，OpenUSD 阶段可重新打开，依赖全部位于新目录内且没有未解析引用；MuJoCo 官方工具成功编译 robot.xml、scene.xml，并分别加载 XML 和 MJB 执行一步零控制仿真。
- 导出前后 13 项 SW 设置记录完全一致。原始模型的 156 个文件 SHA256 全部保持不变。

## 仍需注意

- 首次预览准备耗时约 2 分 8 秒，质量计算期间 SW 界面暂时不响应。导出前的惯性校验也占用约 2 分 25 秒；这仍是性能问题，不能用导出最终成功掩盖。
- 导出报告为 WARN：有 3 项碰撞策略回退和 2 项网格估算偏差警告。SW 实际调整过 STL 参数，插件记录了读回值并继续导出，没有把请求值冒充实际值。
- 日志还出现旧 CSV 写入器的未写入列提示；实际 URDF 结构和惯性校验通过。该日志提示不等同于已解决的导出失败，但仍值得整理。
- 本次设置基线中的两个坐标系偏好为空。非空坐标系恢复、错误路径诊断保留、Mimic 连续保存及深树性能等仍以原修复提交中的回归测试为证据，不声称本次逐项完成了实机故障注入。
- 当前屏幕和窗口尺寸通过不代表全部 DPI、多显示器组合已覆盖，也没有进行 ROS/Gazebo 中的完整机器人运行验收。

## 本地证据

- 导出目录：`E:\桌面\SW2URDF\test-output\fc88a14`
- 安装核验：`.codex-build/review-fixes/installed-verification.json`
- 实机证据：`.codex-build/gui-43392a8/gui-fc88a14-normal/`
- 设置记录：`before-preferences.json`、`after-preferences.json`，两者 SHA256 均为 `43085AAF1A9318BEF16D7CB4D3621A9E7988E0DC3088278289ADB0324FFCE4AB`。
- 文件检查：`live-export-check.json`，包含复制后的目录和 6 次官方 MuJoCo 工具执行结果。
- 设置检查脚本最初因 PowerShell 对 JSON 数字的 decimal/double 转换产生误报。已改成比较同一序列化器生成的原始记录并重跑通过，没有放宽数值容差，也没有修改实际 SW 设置。

以上证据保存在本地，未发布到远端。
