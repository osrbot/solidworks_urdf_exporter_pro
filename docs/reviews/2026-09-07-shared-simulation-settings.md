# 公共仿真设置

## 修改内容

- 模型导出页的“OpenUSD 设置”改为“仿真设置”。选择 OpenUSD 或 MJCF 时都可以打开。
- 公共区配置固定/自由基座，以及每个 Joint 的不生成驱动、位置、速度、力/力矩控制意图。
- OpenUSD 与 MuJoCo / MJCF 使用两个子标签，分别保存增益。切换标签不会把一边的增益复制到另一边。
- 原始配置没有公共设置时保留旧行为。“保持各目标原有行为”不会强制统一旧目标的基座设置。
- 仿真设置保存在根 Link 配置，随通常的 CAD 配置保存和恢复；不会写入零部件的质量属性。损坏设置不能悄悄用默认值继续导出仿真目标。
- MJCF 配置错误只影响 MJCF，OpenUSD 配置错误只影响 OpenUSD；公共意图错误影响消费它的两个仿真目标，不阻止正常 ROS 导出。

## 单位和驱动

公共数据使用 m、kg、s、rad。OpenUSD 沿用 SI 增益到角度制的转换。MJCF 本版只支持直接关节传动，明确写入 `gear="1"`，不开放复杂传动比配置。

- 位置控制：MJCF `position`，填写正刚度和非负阻尼。
- 速度控制：MJCF `velocity`，填写正速度反馈增益。
- 力/力矩控制：MJCF `motor`；OpenUSD 只记录下游运行时控制意图，不伪造等价 DriveAPI。
- MJCF 输出力上限优先使用该目标的显式配置，否则采用有效的 Joint effort 限值。缺少有效限值时拒绝该目标。
- fixed 和 Mimic 从动关节不生成独立主动驱动。没有公共驱动意图的 Joint 默认为不生成驱动，不等于锁死关节。

这不是完整训练环境：不生成地面、奖励、观测、重置、控制程序或自动调参。USD 加速度驱动、复杂执行器和传动参数不在本版新增范围内。

## 验证范围

- Core 138 项测试通过，包含配置 JSON、旧行为、目标参数校验、生成 XML，以及随包 MuJoCo 3.12.0 对生成模型的编译检查。
- 独立的自由基座测试模型验证了 position、velocity、motor 的正负响应和 +/-4.25 输出限幅。此模型由探针构建，不是用户小车的长时间仿真。
- 插件目标配置测试 26 项、配置序列化测试 13 项通过。
- 中英文文档构建通过；JSON Schema 语法检查通过。
- 布局回归的 89 项行为测试通过；新增 4 项中英文、96/144 DPI 预览测试通过，生成两个目标标签页的 8 张截图。
- 最终审查补充目标隔离回归：不能用公共模式覆盖掩盖非法 USD 参数；局部目标配置恢复失败不能影响其他有效目标。

## 远端验收记录

- `6f07b90` 安装包通过 Core 129 项、官方 MuJoCo 9 项、插件 990 项测试。远端安装后核对了 249 个文件和 COM 注册，SolidWorks 正常启动并自动加载插件。
- 使用独立装配体副本，154 个 CAD 文档打开无错误；安装前核对原始文件未改变。不修改 Creo 或原始装配体。
- 实际打开了“仿真设置”，检查公共基座模式、关节驱动意图和两个目标标签页。取消后重开不会保留未应用的输入。
- 实机发现点击“应用”报 `Simulation settings belong to the root Link`。原因是设置读写使用了未初始化的旧 `Exporter.Links` 字段，而原测试人为填充了它，未模拟真实入口。
- `ad84f60` 改为读写向导持有的 `BaseNode.Link`，不修改模型层的根节点约束；同步修正测试数据，并补充旧字段为空时应用、恢复配置的回归测试。
- 修复版第一次全量打包：Core 129 项和官方 MuJoCo 9 项通过；插件 991 项中 989 项通过、2 项失败，未生成安装包。失败项为 Joint 基本页首选高度超出容器，以及 120 DPI 页脚测试线程超过 60 秒。使用同一份 DLL 单独重跑基本页和约束页，2 项通过；不能据此认定全量通过。
- 根节点应用、恢复和目标隔离回归已通过；没有放宽原断言或跳过测试。
- 第二次相同提交全量测试为 990/991 通过，之前两个失败项通过，但 OpenUSD 的自碰撞选项超出父容器。排查发现动态目标标签页参与了首次显示尺寸冻结；`a422ed7` 将这两个标签页排除在固定尺寸缓存之外，补齐选项区自动行高，并增加缓存后仍保持自动布局的测试。未放宽原截图断言或跳过测试。

- `a422ed7` 最终干净打包通过 Core 129/129、官方 MuJoCo 9/9、插件 992/992，均无失败或跳过。前述 Joint 高度、120 DPI 页脚和目标页布局用例在此轮全部通过。
- 安装文件：`sw2urdfSetup_20260907_a422ed7.exe`，SHA-256：`a6b826b06f59f166a22f8b6022cd0007bce5630feff47b5c773bec6eb52b8f3d`。远端安装日志显示成功，COM 注册退出码为 0，已安装 DLL 版本核对为 `a422ed774473`。
- 远端 SolidWorks 2023 SP1 正常启动自动加载新版，154 个测试副本文档全部打开且无错误。再次核对原始 CAD 文件，没有变化。
- 实际将公共基座设为浮动、`left_steering_hinge_joint` 设为位置驱动，OpenUSD 刚度/阻尼为 `80/4`，MJCF 为 `25/2`。点击应用成功，不再出现根 Link 报错；重新打开并切换两个标签，模式及两套独立参数均保留。
- 实际向导四目标带网格导出全部成功：ROS 1、ROS 2、OpenUSD、MJCF，119 个文件、234.27 MiB，用时 1 分 33 秒。输出到新目录 `C:/Users/kitso/Desktop/SW2URDF-simulation-a422ed7`，未覆盖原有 `test` 目录。
- 核对实际 MJCF：存在 `root_freejoint`，位置执行器使用 `kp="25"`、`kv="2"`、`gear="1"`，力矩限幅为 `-1 1`。随包 MuJoCo 3.12.0 成功编译 `robot.xml`、`scene.xml`；原始 XML 和编译后的 MJB 均独立载入并运行一步零控制仿真。
- 核对实际 USD：浮动基座、不生成世界固定关节，1 个有效驱动；输入的 `80/4` 按角度制转换为 `1.3962634/0.06981317`。OpenUSD 0.26.8 重新打开场景并解析了 15 个网格，质量属性和物理关节结构验证通过。未在 Isaac Sim、PhysX 或 Newton 中进行运行验收。
- 完整输出已复制回本地留档。2026-09-07 20:36 再次核对原始 154 个 CAD 文件，哈希全部未变。
- 保存测试副本后正常退出 SolidWorks，再启动新进程并重新打开副本。从真实菜单进入导出向导，确认浮动基座、位置驱动、USD `80/4` 和 MJCF `25/2` 全部恢复。两个目标参数没有串值；复核后取消设置对话框，没有再次导出或修改参数。

验收期间 RustDesk 的键盘传输出现丢键，先前异常输入已取消。本轮切换传统传输，并逐格确认可见数值后才应用；没有将输入未生效当作插件参数校验结果。

## 实机复测步骤

1. 打开测试装配体副本，进入“预览并导出”，到“模型与导出”页打开“仿真设置”。
2. 选择浮动基座，将一个活动关节设为位置控制。点击应用，再次打开设置，确认两个值保留且不再出现根 Link 错误。
3. 分别在 OpenUSD、MJCF 页设置位置或速度控制所需的增益，使用不同数值；切换标签后确认互不覆盖。取消修改后重开，确认未应用的输入不被保存。
4. 同时导出 ROS 1、ROS 2、OpenUSD、MJCF 到新的空目录。检查结果窗口逐项目状态，不能只看总进度完成。
5. 检查 MJCF 的自由基座和 actuator、力限值，使用随包官方 MuJoCo 编译、加载；检查 USD 的基座和驱动属性、单位转换。保留两个目标对 effort 的能力差异说明。
6. 在测试副本上保存配置，完全退出并重新启动 SolidWorks，再打开副本和向导，确认仿真设置恢复。再次核对原始 CAD 文件哈希未改变。

位置/速度的数值调参需要结合负载和控制周期。这里的验收证明配置与导出行为，不代表模型已经完成训练环境或控制器调优。
