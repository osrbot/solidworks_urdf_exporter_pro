<Window x:Class="SW2URDF.UI.LinkTreeCanvas.LinkTreeOutlineWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Link 树大纲编辑"
        Width="960" Height="720" MinWidth="760" MinHeight="520"
        WindowStartupLocation="CenterOwner"
        Background="#F4F6F8"
        FontFamily="Microsoft YaHei UI" FontSize="13">
    <Window.Resources>
        <SolidColorBrush x:Key="PanelBorder" Color="#D9DEE5" />
        <Style TargetType="Button">
            <Setter Property="MinHeight" Value="32" />
            <Setter Property="Padding" Value="14,5" />
            <Setter Property="Margin" Value="0,0,8,0" />
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="72" />
            <RowDefinition Height="*" />
            <RowDefinition Height="52" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="White" BorderBrush="{StaticResource PanelBorder}" BorderThickness="0,0,0,1">
            <StackPanel Margin="20,12">
                <TextBlock Text="Link 树大纲编辑" FontWeight="SemiBold" FontSize="18" />
                <TextBlock Text="每行一个 Link，# 的数量表示层级。修改通过校验后才会返回画布。"
                           Margin="0,5,0,0" Foreground="#667085" />
            </StackPanel>
        </Border>

        <Grid Grid.Row="1" Margin="16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="280" />
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="White" BorderBrush="{StaticResource PanelBorder}" BorderThickness="1">
                <TextBox x:Name="OutlineTextBox"
                         AcceptsReturn="True" AcceptsTab="True"
                         VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto"
                         TextWrapping="NoWrap" SpellCheck.IsEnabled="False"
                         FontFamily="Consolas" FontSize="15"
                         Padding="14" BorderThickness="0"
                         TextChanged="OutlineTextChanged" />
            </Border>

            <Border Grid.Column="1" Margin="14,0,0,0" Background="White"
                    BorderBrush="{StaticResource PanelBorder}" BorderThickness="1">
                <Grid Margin="16">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <TextBlock Text="写法" FontWeight="SemiBold" FontSize="15" />
                    <TextBlock Grid.Row="1" Margin="0,10,0,0" FontFamily="Consolas" LineHeight="23"
                               Text="# base_link&#x0a;## camera_link&#x0a;## steering_link&#x0a;### wheel_link" />
                    <TextBlock Grid.Row="2" Margin="0,16,0,0" Foreground="#475467" TextWrapping="Wrap"
                               Text="同名 Link 会保留原有 Joint、参数和 CAD 绑定。新增 Link 自动创建 fixed Joint，例如 camera_link 生成 camera_joint；Joint 细节可回画布修改。删除一行会从候选树中删除该 Link；名称必须符合 ROS 命名规则。" />
                    <ScrollViewer Grid.Row="3" Margin="0,18,0,0" VerticalScrollBarVisibility="Auto">
                        <TextBlock x:Name="ValidationText" TextWrapping="Wrap" Foreground="#C62828" />
                    </ScrollViewer>
                </Grid>
            </Border>
        </Grid>

        <Border Grid.Row="2" Background="White" BorderBrush="{StaticResource PanelBorder}" BorderThickness="0,1,0,0">
            <Grid Margin="16,0">
                <TextBlock x:Name="StatusText" VerticalAlignment="Center" Foreground="#344054" />
                <StackPanel HorizontalAlignment="Right" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button Content="恢复当前树" Click="ResetClick" />
                    <Button Content="取消" Click="CancelClick" />
                    <Button x:Name="ApplyButton" Content="更新画布" Margin="0" Click="ApplyClick" />
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
